package Questions;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Stream_findNth {

	/*
	 * public static void main(String[] args) { ArrayList<Students> s1 = new
	 * ArrayList<Students>(); s1.add(new Students("abc" ,101 ,97,"UP")); s1.add(new
	 * Students("def" ,102 ,95,"DL")); s1.add(new Students("ghi" ,103 ,96,"MP"));
	 * s1.add(new Students("jkl" ,104 ,56,"MH")); s1.add(new Students("mno" ,105
	 * ,76,"RJ")); s1.add(new Students("pqr" ,106 ,75,"TL")); s1.add(new
	 * Students("stu" ,107 ,34,"AP")); s1.add(new Students("vwx" ,108 ,41,"WB"));
	 * 
	 * 
	 * 
	 * 
	 * Double dm = s1.stream().map((a) -> a.getMarks()).max(Double ::compare).get();
	 * 
	 * / Double hm=s1.stream().map((e) -> e.getMarks()).max(Double ::compare).get();
	 * s1.stream().filter((e) -> e.getMarks()==hm).forEach(a ->
	 * System.out.println(a.getName()));
	 * 
	 * } }
	 * https://stackoverflow.com/questions/17775384/categorize-arraylist-of-objects-
	 * based-on-their-property
	 * 
	 * { employee.setAmount(lb.get(i).getAmount());
	 * employee.setName(lb.get(i).getEmpName()); lemp.add(employee); } for(Employee
	 * emp : lemp) { } class Students{ public Students(String name, int rollno,
	 * double marks, String state) { this.name = name; this.rollno = rollno;
	 * this.marks = marks; this.state = state; }
	 * 
	 * @Override public String toString() { return "Students [name=" + name +
	 * ", rollno=" + rollno + ", marks=" + marks + ", state=" + state + "]"; }
	 * String name; int rollno; double marks; String state; public String getName()
	 * { return name; }
	 * 
	 * public int getRollno() { return rollno; }
	 * 
	 * public double getMarks() { return marks; }
	 * 
	 * public String getState() { return state; }
	 */
}